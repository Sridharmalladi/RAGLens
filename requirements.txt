# Embeddings & retrieval — torch required by sentence-transformers (BGE models, ~400 MB)
torch>=2.5.0
sentence-transformers>=3.1.0
faiss-cpu>=1.8.0
rank_bm25>=0.2.2

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.14.0
python-multipart>=0.0.7

# HF Spaces deployment (sdk: gradio) — mounts our FastAPI app so HF can serve it
gradio>=4.44.0

# Scheduling & storage
apscheduler>=3.10.0

# Groq SDK — generation + evaluation (no local LLM weights needed)
groq>=0.11.0

# Env
python-dotenv>=1.0.0
